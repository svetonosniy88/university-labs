#pragma once

#include "element_array.h"

// Безопасно считывает одно целое число с клавиатуры.
// Повторяет ввод, пока пользователь не введёт корректный int.
int read_int();

// Считывает два целых числа как одну пару.
// При ошибке очищает поток и предлагает повторить ввод пары целиком.
void read_pair(int& p, int& q);

// Выводит все текущие компоненты связности.
void print_groups(const ElementArray& elements);
